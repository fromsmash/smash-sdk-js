{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "release": "lerna publish --contents dist from-package --no-private --git-head ${CODEBUILD_RESOLVED_SOURCE_VERSION} --yes"
  },
  "devDependencies": {
    "@lerna-lite/cli": "^1.11.3",
    "@lerna-lite/run": "^1.11.3",
    "@types/jest": "^28.1.8",
    "@types/node": "^18.7.18",
    "jest": "^28.0.8",
    "jest-extended": "^0.11.5",
    "nx": "^14.7.8",
    "ts-jest": "^28.0.8",
    "typescript": "^4.8.3"
  }
}
