{
	"name": "@smash-sdk/domain",
	"version": "0.0.7",
	"description": "Smash Sdk domain",
	"repository": {
		"type": "git",
		"url": "https://github.com/fromsmash/smash-sdk-js"
	},
	"scripts": {
		"transpile": "tsc",
		"clean": "rimraf dist && rimraf node_modules",
		"build": "npm run clean && npm i && npm run transpile && cp ./package.json ./dist/",
		"test": "jest"
	},
	"dependencies": {
		"@smash-sdk/core": "^1.0.0"
	},
	"keywords": [
		"smash",
		"sdk",
		"domain"
	]
}
